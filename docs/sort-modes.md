# 排序模式

sortTasks 支持四种模式，全部返回新数组且不修改入参。

| 模式 | 规则 |
|---|---|
| priority（默认） | high → medium → low，再按创建时间与 ID |
| due | 有截止日者按日期升序，未设截止日排在最后 |
| newest | 创建时间倒序 |
| title | 按 zh-CN 区域设置比较标题 |

并列时的次级规则一致：先比创建时间（新的在前），再比任务 ID，
保证同一输入在任何环境下得到稳定顺序。
