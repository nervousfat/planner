{
  "name": "demo-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "任务创建、状态流转、优先级与截止日期管理",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check core.js && node --check app.js && node --check server.mjs && node --check history.mjs",
    "history": "node history.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
